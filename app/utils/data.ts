// ~/utils/data.ts
export const normKey = (s: string) => s.toLowerCase().replace(/[^a-z0-9]+/g,'')
export function pickKey(obj:any, keys:string[]){ /*…pakai versi terakhir yang sudah work…*/ }
export function canonStatus(s:any=''){ /*…pakai versi MapView/index…*/ }